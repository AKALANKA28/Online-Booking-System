# name: Deploy API Gateway

# run-name: "${{ github.actor }} deploying API Gateway from ${{ github.ref_name }}"

# on:
#   push:
#     branches: [master]
#     paths:
#       - 'backend/api-gateway/**'
#       - 'backend/pom.xml'
#       - '.github/workflows/deploy-api-gateway.yml'

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Show deployment context
#         run: |
#           echo "Starting deployment for API Gateway"
#           echo "Branch: ${{ github.ref_name }}"
#           echo "Repository: ${{ github.repository }}"
#           echo "Triggered by: ${{ github.event_name }}"

#       - name: Log in to Azure
#         uses: azure/login@v2
#         with:
#           creds: ${{ secrets.AZURE_CREDENTIALS }}

#       - name: Build and deploy API Gateway
#         uses: azure/container-apps-deploy-action@v1
#         with:
#           appSourcePath: ${{ github.workspace }}/backend
#           dockerfilePath: api-gateway/Dockerfile
#           acrName: ${{ secrets.ACR_NAME }}
#           containerAppName: api-gateway
#           resourceGroup: ${{ secrets.AZURE_RESOURCE_GROUP }}
#           containerAppEnvironment: ${{ secrets.AZURE_CONTAINERAPPS_ENV }}
#           imageToBuild: ${{ secrets.ACR_NAME }}.azurecr.io/api-gateway:${{ github.sha }}
#           disableTelemetry: true

#       - name: Deployment complete
#         run: echo "API Gateway deployed successfully."
