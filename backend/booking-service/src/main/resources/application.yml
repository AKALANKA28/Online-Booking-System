server:
  port: ${SERVER_PORT:8083}

debug: ${APP_DEBUG:true}

spring:
  application:
    name: booking-service
  datasource:
    url: ${BOOKING_DB_URL:jdbc:postgresql://ep-morning-meadow-a8vv6di9-pooler.eastus2.azure.neon.tech/bookingdb?&sslmode=require&channelBinding=require}
    username: ${BOOKING_DB_USERNAME:neondb_owner}
    password: ${BOOKING_DB_PASSWORD:npg_cd8jbG0HYQnq}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:rabbitmq}
    port: ${SPRING_RABBITMQ_PORT:5672}
    username: ${SPRING_RABBITMQ_USERNAME:guest}
    password: ${SPRING_RABBITMQ_PASSWORD:guest}
    virtual-host: ${SPRING_RABBITMQ_VHOST:/}
    ssl:
      enabled: ${SPRING_RABBITMQ_SSL_ENABLED:false}

services:
  seat:
    base-url: ${SERVICES_SEAT_BASE_URL:http://localhost:8082}
  payment:
    base-url: ${SERVICES_PAYMENT_BASE_URL:http://localhost:8084}

app:
  rabbitmq:
    exchange: ticketing.events
    event-cancelled-queue: booking.event.cancelled.queue
  security:
    internal-api-key: ${INTERNAL_API_KEY:internal-demo-secret}

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
