server:
  port: ${SERVER_PORT:8082}

debug: ${APP_DEBUG:true}

spring:
  application:
    name: seat-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-morning-meadow-a8vv6di9-pooler.eastus2.azure.neon.tech/seatdb?&sslmode=require&channelBinding=require}
    username: ${SPRING_DATASOURCE_USERNAME:neondb_owner}
    password: ${SPRING_DATASOURCE_PASSWORD:npg_cd8jbG0HYQnq}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  rabbitmq:
    addresses: ${SPRING_RABBITMQ_ADDRESSES:amqps://jpwpeqpo:Fde1Gvx2RIHSydGr2KMT0XuZVCA9M7ju@yak.lmq.cloudamqp.com/jpwpeqpo}
    username: ${SPRING_RABBITMQ_USERNAME:jpwpeqpo}
    password: ${SPRING_RABBITMQ_PASSWORD:Fde1Gvx2RIHSydGr2KMT0XuZVCA9M7ju}
    virtual-host: ${SPRING_RABBITMQ_VHOST:jpwpeqpo}
    ssl:
      enabled: ${SPRING_RABBITMQ_SSL_ENABLED:false}

app:
  rabbitmq:
    exchange: ticketing.events
    queue: seat.event.created.queue
    cancelled-queue: seat.event.cancelled.queue
  security:
    internal-api-key: ${INTERNAL_API_KEY:internal-demo-secret}
  hold-duration-minutes: 10

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
